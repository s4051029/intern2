<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/news_background"
                tools:context="com.mirrorchannelth.internship.fragment.TaskHistoryFragment">
    <include layout="@layout/layout_collapse_toolbar" android:id="@+id/userToobar"/>
    <ProgressBar
        android:layout_width="wrap_content"
        android:id="@+id/progressBar"
        android:layout_centerInParent="true"
        android:visibility="gone"
        android:indeterminateDrawable="@drawable/progressbar"
        android:layout_height="wrap_content"/>
    <com.aspsine.irecyclerview.IRecyclerView
        android:id="@+id/userRecycleview"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_below="@id/userToobar"
        android:layout_height="match_parent"/>

    <android.support.design.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/coordinatorLayout"
        android:layout_below="@id/userToobar"
        android:visibility="gone"
    >

    </android.support.design.widget.CoordinatorLayout>
    <ProgressBar
        android:layout_width="wrap_content"
        android:id="@+id/taskProgressBar"
        android:layout_centerInParent="true"
        android:visibility="gone"
        android:indeterminateDrawable="@drawable/progressbar"
        android:layout_height="wrap_content"/>

</RelativeLayout>
